# Images & Assets

How Razor Designer resolves image paths.

!!! note "Stub"
    Expand. The designer walks the loaded library's `Assets/` directory when
    resolving image paths (see commit `2a5ff06`). Document the lookup order,
    supported formats, and how to add custom images.
